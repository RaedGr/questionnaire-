import {msg} from './Personality';
const res= {msg}
   export default function  Result() {
    return ( 
    <div>        if (res==='1')
        {
            <h1> Qraed</h1>

        }else if(res==='2') 
        {
            <h1> Qraed12</h1>
        }else if(res==='3'){

        }else {
<h1> Qraed13451354</h1>
        }
        

        <button className="next-button" onClick={() => window.print()}>
          Download as PDF
        </button>
        </div>
    )
   }
   